{
  "name": "xtai-tiptap-editor",
  "private": true,
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsup": "^8.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "vitest": "^1.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
